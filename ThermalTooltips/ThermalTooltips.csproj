<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyTitle>Thermal Tooltips</AssemblyTitle>
    <FileVersion>3.0.0.0</FileVersion>
    <RootNamespace>PeterHan.ThermalTooltips</RootNamespace>
    <Description>Adds additional information to tooltips on the Thermal overlay.</Description>
    <AssemblyVersion>3.0.0.0</AssemblyVersion>
    <CopyPreview>true</CopyPreview>
    <Platforms>Vanilla;Spaced Out</Platforms>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Platform)' == 'Vanilla' ">
    <SupportedContent>VANILLA_ID, EXPANSION1_ID</SupportedContent>
    <LastWorkingBuild>466655</LastWorkingBuild>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Platform)' == 'Spaced Out' ">
    <ArchivedVersionPath>\archived_versions\merged</ArchivedVersionPath>
    <SupportedContent>EXPANSION1_ID</SupportedContent>
    <LastWorkingBuild>468097</LastWorkingBuild>
  </PropertyGroup>
</Project>
